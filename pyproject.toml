[project]
name = "tracemem"
version = "0.1.0"
description = "Knowledge graph memory system for AI agents"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "tracemem_core",
    "tracemem-installer",
]
